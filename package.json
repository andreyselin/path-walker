{
  "name": "path-walker",
  "version": "1.0.0",
  "description": "Utility class for moving objects along 2d path",
  "main": "index.js",
  "scripts": {
    "build0": "rm -rf types && tsc",
    "build": "rm -rf types && rm -rf dist && tsc --project tsconfig.build.json"
  },
  "dependencies": {
    "typescript": "^4.4.2"
  },
  "types": "./types",
  "author": "Andrey Selin",
  "license": "ISC"
}
